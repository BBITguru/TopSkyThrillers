function getDeck() {
  return {
    // get 1 random blackCard from promptCard
    black: blackCard(),
    
    // get 5 random white cards from responseCard for each user
    white: whiteCard()
  };
}

function blackCard() {
  return [
    
  ];
};

function whiteCard() {
  return [

  ];
};